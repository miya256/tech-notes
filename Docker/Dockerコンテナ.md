## コンテナに入っているもの
- プログラムのコード
- 実行環境（プログラミング言語）
- 必要なライブラリ
- ファイルシステム

runすると、イメージからコンテナが作られ、そのコンテナの中でコマンドが実行される。  
コンテナの中で何のコマンドを実行するかは、Dockerfile？

コードはイメージに入っている状態のコードがつかわれる。  
ローカルの状態を反映したいなら、マウントってのをするらしい。

buildでDockerfileに書かれた手順にしたがってイメージが作られる
