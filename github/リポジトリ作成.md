### ブラウザ
- githubでnewする
### ローカル
- 対象フォルダまで移動
- 必要なら.gitignoreをつくる
- コマンドを実行
```
git init
git remote add origin リポジトリのURL
git branch -M main

// ブラウザでREADMEや.gitignoreを作っていたらpull
git pull origin main --allow-unrelated-histories

git add .
git commit -m "message"
git push -u origin main
```
  