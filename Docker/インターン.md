docker-compose.ymlにvolumesてのを書くと、ローカルのコード変更を即反映

DockerfileにRUN uv syncとあり、uv syncしてるっぽい。だからコンテナの中のvenvの中につくられる？
なので、コンテナの中で`uv run python script.py`すると動く。
uv runすればactivateしなくてよい

起動
`docker compose up -d`
中に入る
`docker compose exec app bash`
もしbashがないなら
`docker compose exec app sh`
